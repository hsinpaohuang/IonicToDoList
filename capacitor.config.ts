import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hsinpaohuang.todolist',
  appName: 'IonicToDoList',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
